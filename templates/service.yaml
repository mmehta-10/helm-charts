apiVersion: v1
kind: Service
metadata:
  name: {{ include "nettest.fullname" . }}
  labels:
    {{- include "nettest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nettest.selectorLabels" . | nindent 4 }}
